import buildGlobalStyles from './style/buildGlobalStyles';
import padScale from './utils/padScale';
import theme from './style/theme';

module.exports = {
  buildGlobalStyles,
  padScale,
  theme,
};
